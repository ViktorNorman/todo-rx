# Testing out a React workflow with rxjs keeping state in a redux-manner.

Built redux -ish state management using rxjs but mainly testing the workflow of utilizing rxjs in a friendly manner using hooks.
